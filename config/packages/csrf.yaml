# Classic session CSRF for forms - no JS cookie dance required.
# (Skeleton defaults + Turbo made browser POSTs return 422 with a blank-looking page.)
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - authenticate
            - logout
